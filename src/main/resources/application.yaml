spring:
  application:
    name: congestion-tax-calculator
  profiles:
    default: dev

management:
  endpoint:
    health:
      show-components: always
      show-details: never

logging:
  level:
    root: INFO